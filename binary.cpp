#include <bits/stdc++.h>
using namespace std;
int main(int argc, char const *argv[])
{
	int n;
	cin>>n;
	for(int i=0;i<n;++i)
	{
		cin>>array[i];
	}
	int number, mid=n/2, start=0, end=n-1;
	cin>>number;
	while(number!=array[mid])
	{
		if(number>array[mid])
		{
			
		}
	}
	return 0;
}